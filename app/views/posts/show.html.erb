<%= @post.content %>

<%= link_to "Delete", post_path(@post), method: :delete %>

<%= form_for [@post, @post.comments.build] do |f| %>
  <%= f.text_field :reply, placeholder: 'Reply' %>
  <%= f.submit "Reply!" %>
<% end %>



<% @post.comments.each do |comment| %>
  <%= comment.reply %>
<% end %>
